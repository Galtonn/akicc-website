{
  "name": "akicc-website",
  "version": "1.0.0",
  "description": "AKICC - Printer sales and maintenance services website",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm install && npm run build"
  },
  "keywords": [
    "printer",
    "maintenance",
    "sales",
    "akicc"
  ],
  "author": "AKICC",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "dotenv": "^17.2.0",
    "nodemailer": "^7.0.5",
    "pg": "^8.16.3"
  }
}
